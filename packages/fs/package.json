{
  "name": "@opencode-channels/fs",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@opencode-channels/core": "workspace:*",
    "@opencode-ai/plugin": "^1.2.10",
    "hono": "^4.12.0"
  },
  "devDependencies": {
    "vitest": "^3.2.1",
    "typescript": "^5.7.0"
  }
}
